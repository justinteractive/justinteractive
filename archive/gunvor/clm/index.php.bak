<?php 
    $column_headers = ['Delivery From', 'Delivery To'];
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bootstrap 5 Data Table</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
        }
        .table-container {
            overflow-x: auto;
            padding: 20px;
        }
        .table thead th {
            font-weight: bold;
            font-size: 12px;
            border-left: 1px solid #dee2e6;
            text-align: left;
        }
        .table tbody td {
            font-size: 12px;
            text-align: left;
        }
        .table tbody tr {
            border-bottom: 1px solid #dee2e6;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h2>Data Table Title</h2>
        </div>
        <div class="mb-2">
            <button class="btn btn-primary">Action 1</button>
            
        </div>
        <div class="table-container border rounded p-2">
            <table class="table table-striped table-hover">
                <thead class="table-light">
                    <tr>
                        <!-- Generate 28 columns -->
                        <?php foreach($column_headers AS $header) { ?>
                            <th><?= $header; ?></th>
                        <?php } ?>
                    </tr>
                </thead>
                <tbody>
                    <!-- Generate 40 rows -->
                    " + "".join(f"""
                    <tr>
                        <td>{row}</td>
                        {''.join(f'<td>Data {row}-{col}</td>' for col in range(1, 28))}
                    </tr>
                    """ for row in range(1, 41)) + "
                </tbody>
            </table>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
